# local dev & design assets — deliberately not published
config.js
tests/
package.json
package-lock.json
council/
# design/ stays unpublished — EXCEPT the celebration prototype, which the owner
# approved and asked to have version-controlled (2026-09-11). It had existed on
# one machine only; a clean checkout could not have produced it.
design/*
!design/celebration/
design/celebration/*.ckpt-*
supabase/
DESIGN.md
CLAUDE.md
.DS_Store
.gstack/
HANDOVER.md
serve-dev.py
.foreman/
dev-crews.json
# real Supabase creds, parked while the dev backend is in use
config.js.parked
# release runbook, local only
PUBLISH.md
# local safety copies
*.bak-*
# local editor/tooling config
.claude/

# Cloudflare local dev state (the API itself, worker/, is committed)
worker/.wrangler/
